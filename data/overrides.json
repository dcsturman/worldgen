{
  "_comment": "Curated per-system facts, merged into generation. See src/systems/overrides.rs for the schema. Validate with: cargo run --bin validate-overrides --features backend",
  "overrides": []
}
